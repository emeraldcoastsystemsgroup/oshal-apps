{
  "// why": "Compile the embodied package routes AND its bundled deterministic simulation engine to CommonJS in ../routes with @/ aliases LEFT INTACT (the oshal loader resolves @/ at runtime — BUILDING-EXTENSIONS §5). Framework `@/` types come from core-modules.d.ts (ambient, never emitted). express/node types resolve from the core checkout's node_modules (declarations only, never emitted). sourceMap is ON to match the framework's canonical emit (the `//# sourceMappingURL` trailer is part of the reviewed smoke-module bytes) — delete the .js.map files after building, never commit them. Run: node C:/Projects/oshal/node_modules/typescript/bin/tsc -p c:/Projects/oshal-apps/embodied/src-routes/tsconfig.json && find ../routes -name '*.js.map' -delete",
  "compilerOptions": {
    "module": "commonjs",
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "moduleResolution": "node",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "strict": true,
    "noUnusedLocals": true,
    "sourceMap": true,
    "outDir": "../routes",
    "rootDir": ".",
    "baseUrl": ".",
    "paths": {
      "express": ["C:/Projects/oshal/node_modules/@types/express"]
    },
    "typeRoots": ["C:/Projects/oshal/node_modules/@types"],
    "types": ["node"]
  },
  "include": ["**/*.ts", "**/*.d.ts"]
}
