{
  "// why": "Compile the animatronics package routes AND its bundled deterministic engine to CommonJS in ../routes with @/ aliases LEFT INTACT (the oshal loader resolves @/ at runtime — BUILDING-EXTENSIONS §5). Framework `@/` types come from core-modules.d.ts (ambient, never emitted). express/node types resolve from the core checkout's node_modules (declarations only, never emitted). sourceMap is OFF: the canonical store rebuild strips the sourceMappingURL trailer and its check compares stripped bytes, so nothing to delete. Run: node C:/Projects/oshal/node_modules/typescript/bin/tsc -p c:/Projects/oshal-apps/animatronics/src-routes/tsconfig.json",
  "compilerOptions": {
    "module": "commonjs",
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "moduleResolution": "node",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "strict": true,
    "noUnusedLocals": true,
    "sourceMap": false,
    "outDir": "../routes",
    "rootDir": ".",
    "baseUrl": ".",
    "paths": {
      "express": ["C:/Projects/oshal/node_modules/@types/express"]
    },
    "typeRoots": ["C:/Projects/oshal/node_modules/@types"],
    "types": ["node"]
  },
  "include": ["**/*.ts", "**/*.d.ts"]
}
