# tools/

The app's bundled tools (`audioTranscribeTool.js`, `studyContentTool.js`,
`pdfIngestTool.js`, `classKnowledgeTool.js` + static HTML), discovered from this dir at
load time (`../oshal-app.yaml → toolsDir: tools/`). **Produced by the extraction build —
see [../BUILD.md](../BUILD.md).**
